version: "2"
sql:
  - engine: "postgresql"
    queries: "business/transactionbus/stores/transactiondb/queries"
    schema: "business/transactionbus/stores/transactiondb/schemas"
    gen:
      go:
        package: "transactiondb"
        out: "business/transactionbus/stores/transactiondb/gen"
        sql_package: "pgx/v5"
        emit_interface: true
        emit_json_tags: true
        json_tags_case_style: "camel"
        overrides:
          - db_type: "timestamptz"
            go_type: "time.Time"
          - db_type: "timestamp"
            go_type: "time.Time"
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"